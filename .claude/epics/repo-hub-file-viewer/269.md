---
name: Wire import success forward action
status: open
created: 2026-06-11T18:25:12Z
updated: 2026-06-11T18:40:00Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/269
depends_on: [268]
parallel: true
conflicts_with: []
---

# Task: Wire import success forward action

## Description

Close the M11 dead-end: the import success view
(`apps/web/app/import/_components/import-flow.tsx`, `SuccessView`) currently
offers only "Import another repository". Add the forward action into the
imported repo now that the viewer (#268) and hub (#267) exist.

## Acceptance Criteria

- [ ] `SuccessView` gains a primary forward action linking to the imported
      repo's file viewer (`/repos/[owner]/[repo]/files` per the #266 URL
      contract), using the `owner`/`repo` already present in
      `ImportSuccessView`.
- [ ] Each captured key file in the success list links to the viewer at that
      file's path (deep link per the URL contract).
- [ ] "Import another repository" remains; the new action follows the
      import feature's existing button/link styling (`import.css`).
- [ ] `pnpm lint` / `typecheck` / web `build` pass.

## Technical Details

- Touch only `apps/web/app/import/` (component + stylesheet if a class is
  genuinely needed). Do not modify the viewer or hub.
- `SuccessView` is in a Client Component file — use `next/link`.

## Dependencies

- depends_on: #268 (viewer URL contract live on the epic branch).

## Effort Estimate

- Size: S
- Hours: 1

## Definition of Done

- [ ] Code implemented
- [ ] Lint/typecheck/build pass
- [ ] Code reviewed
