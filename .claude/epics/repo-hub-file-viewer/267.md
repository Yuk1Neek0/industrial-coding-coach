---
name: Implement /repos hub page
status: open
created: 2026-06-11T18:25:12Z
updated: 2026-06-11T18:40:00Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/267
depends_on: [265]
parallel: true
conflicts_with: []
---

# Task: Implement /repos hub page

## Description

Replace `apps/web/app/repos/page.tsx` (M16's redirect placeholder) with the
real Repos Hub per the Page Spec from #265: a Server Component listing
imported snapshots with links into each repo's coaching areas.

## Acceptance Criteria

- [ ] `/repos` renders the hub per `docs/design/repos-hub-page.page-spec.md`
      — the redirect is gone (and the redirect's "follow-up" comment with
      it).
- [ ] Rows come from `listImportedRepos` (`@workspace/db`); each row shows
      owner/repo, ref, imported time, file count and links to the per-repo
      areas with the spec's exact hrefs (Files links target the #268 route —
      fine to ship before #268 merges within the same epic PR).
- [ ] Empty state per spec with CTA to `/import`.
- [ ] Shared `AppNav` (`active="repos"`); repos feature styling conventions;
      `loading.tsx`/`error.tsx` consistent with sibling repos pages.
- [ ] `pnpm lint` / `typecheck` / web `build` pass.

## Technical Details

- Server Component reading SQLite only — mirror how sibling repos pages
  construct the db handle (see `repos/[owner]/[repo]/issues/page.tsx`).
- Keep the page self-contained (`_components/` if needed); do not touch the
  viewer route (#268) or the import flow (#269).

## Dependencies

- depends_on: #265 (Page Spec).

## Effort Estimate

- Size: M
- Hours: 2.5

## Definition of Done

- [ ] Code implemented
- [ ] Lint/typecheck/build pass
- [ ] Code reviewed
