---
name: Observability page — Page Spec + Claude Design prompt
status: open
created: 2026-06-02T18:26:58Z
updated: 2026-06-02T19:14:08Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/226
depends_on: [225]
parallel: true
conflicts_with: []
---

# Task: Observability page — Page Spec + Claude Design prompt

## Description

Write the Page Spec for the **Observability** page (`/observability/[owner]/[repo]`)
under `docs/design/`, then the Claude Design prompt — the CLAUDE.md-mandated step
before any UI (ADR 0007). The page shows BOTH halves: Part A (the snapshot's
coach-call traces, eval pass-rates, cost/latency) and Part B (the repo
observability story + teaching, with the `absent` educational state).

## Acceptance Criteria
- [ ] Page Spec saved under `docs/design/` following the M12
      `delivery-traceability-page.page-spec.md` shape (sections 1–14).
- [ ] Defines the Part-A panel (a traces table/list + per-call aggregate cards:
      count, eval pass-rate, est. cost, avg latency) with an honest "estimated
      cost" label, and the empty "no calls traced yet" state.
- [ ] Defines the Part-B panel (the observability story + teaching) and the
      `absent` educational state.
- [ ] Read-only, **no token/API key/network at view**; beginner-first copy, no
      raw HTTP codes / stack traces.
- [ ] Grounded in the task-7 `getObservability` result shape.
- [ ] Claude Design prompt written + saved under `docs/design/ui-prompts/`.
- [ ] No `apps/web` code in this task.

## Technical Details
- Docs only: `docs/design/` (+ `ui-prompts/`). Conflicts with nothing.
- Reference the task-7 result shape so the design covers both panels + the empty /
  absent states. Note the M11 brand-icon lesson if a glyph is wanted.

## Dependencies
- 7 (the `getObservability` result shape). Can overlap Wave 2/3.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
