---
name: Delivery page — Page Spec + Claude Design prompt
status: closed
created: 2026-06-02T13:21:59Z
updated: 2026-06-02T15:47:32Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/204
depends_on: [200]
parallel: true
conflicts_with: []
---

# Task: Delivery page — Page Spec + Claude Design prompt

## Description

Write the Page Spec for the **Delivery Traceability** page (`/delivery/[owner]`)
under `docs/design/`, then the Claude Design prompt — the CLAUDE.md-mandated step
before any UI work (v0/Claude Design, ADR 0007). The spec covers both states: the
populated traceability map (PRD → epic → task → issue/PR with per-node teaching)
and the graceful-degradation "no spec-driven workflow detected" educational view
linking to the M2 Golden Path. No app code in this task — spec + prompt only.

## Acceptance Criteria
- [ ] Page Spec saved under `docs/design/` following the shape of existing specs
      (e.g. the M11 Import and M7/M9 page specs).
- [ ] Spec defines BOTH states: populated map and degradation/educational state,
      with beginner-first copy rules and NO raw HTTP codes.
- [ ] Spec defines the map interaction (navigate PRD→epic→task, view per-node
      teaching, see issue/PR link status incl. "not synced" / "link failed").
- [ ] Spec is grounded in the 004 map shape (nodes/edges/states it must render).
- [ ] Claude Design prompt written and saved alongside the spec / in
      `docs/design/ui-integration-notes/` per the existing convention.
- [ ] No `apps/web` code changes in this task.

## Technical Details
- Docs only: `docs/design/` (+ `ui-integration-notes/`). Conflicts with nothing.
- Reference the 004 discriminated-union result so the design covers the absent
  state explicitly (the common case for the target user).
- Note the brand-icon lesson from M11 (lucide-react dropped brand glyphs) if the
  design wants a GitHub mark.

## Dependencies
- 004 (map shape). Can overlap Wave 2 once the graph result type is settled.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
