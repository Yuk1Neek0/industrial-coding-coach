---
name: Author 15 template entries + seed script + referential-integrity test
status: closed
created: 2026-05-21T00:46:18Z
updated: 2026-05-21T19:38:55Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/46
depends_on: [44]
parallel: true
conflicts_with: []
---

# Task: Author 15 template entries + seed script + referential-integrity test

## Description

Hand-author all 15 template entries from the milestone plan, wire them into a
reproducible seed, and add the referential-integrity test that proves every
`templatesReferenced` slug in the M2 catalog resolves to a template (PRD FR-2,
FR-6). This is the heaviest task in the epic.

## Acceptance Criteria
- [ ] 15 template entries authored: shadcn/ui monorepo, create-next-app, T3
      stack, claude-code-templates, CCPM, GitHub Spec Kit, BMAD, GitHub Actions
      Node CI, CodeQL, Gitleaks, Dependabot, ADR template, PRD template, OpenAPI
      contract-first template, Langfuse integration starter.
- [ ] Slugs match those used in `goldenPathSeed.templatesReferenced` (11 are
      already referenced; 4 are new: `github-actions-node-ci`, `codeql`,
      `gitleaks`, `dependabot`).
- [ ] Every entry fully populated — no empty explanation fields; each has a
      category and structured fit data.
- [ ] The seed script loads all 15 entries reproducibly.
- [ ] A test asserts every `templatesReferenced` slug in `goldenPathSeed`
      resolves to a seeded template.

## Technical Details

- New `template-seed-data.ts` in `packages/db/src/`, mirroring `seed-data.ts`.
- Extend `seed.ts` to load templates alongside Golden Paths.
- Entries are hand-written and reviewed — NOT generated by an unreviewed LLM call.

## Dependencies

- Task 001 (schema + migration).
- Runs in parallel with task 002 (different files).

## Effort Estimate
- Size: L
- Hours: 8

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
