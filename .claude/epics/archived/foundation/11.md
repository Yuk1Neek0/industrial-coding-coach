---
name: CI + security baseline
status: closed
created: 2026-05-20T17:45:53Z
updated: 2026-05-20T18:19:42Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/11
depends_on: []
parallel: true
conflicts_with: []
---

# Task: CI + security baseline

## Description

Add GitHub Actions CI and a security baseline, derived from official/mature
templates and customized to the monorepo, so quality gates exist before
product work begins.

## Acceptance Criteria
- [ ] `.github/workflows/ci.yml` runs `pnpm install --frozen-lockfile`,
      `pnpm lint`, `pnpm build` on PRs and pushes to `main`
- [ ] `.github/workflows/security.yml` runs Gitleaks
- [ ] `.github/workflows/codeql.yml` runs CodeQL for JavaScript/TypeScript
- [ ] `.github/dependabot.yml` covers npm + GitHub Actions
- [ ] `.gitleaks.toml` exists
- [ ] `.env` is git-ignored; `.env.example` committed; no secrets committed
- [ ] CI files reviewed by a human

## Technical Details

Inspect `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `turbo.json`,
`apps/web/package.json`, `packages/ui/package.json` before writing CI. Pin the
pnpm version to match `packageManager` in root `package.json`. Test steps are
prepared but conditional until test scripts exist.

## Dependencies
Depends on 001 (monorepo) and 005 (`.env.example`).

## Effort Estimate
- Size: M
- Hours: 3

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing (n/a — CI workflows; validated by a CI run)
- [ ] Code reviewed
