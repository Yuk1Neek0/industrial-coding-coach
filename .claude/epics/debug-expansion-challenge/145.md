---
name: Challenge Detail Page — Page Spec + Claude Design prompt (inline collapsible prior-attempts panel)
status: open
created: 2026-05-24T19:59:27Z
updated: 2026-05-24T20:32:00Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/145
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Challenge Detail Page — Page Spec + Claude Design prompt (inline collapsible prior-attempts panel)

## Description

Author the Page Spec and the Claude Design prompt for the Challenge
Detail Page — the UI hand-off gate for M9's per-challenge view. Per
ADR 0007, no UI is generated before a Page Spec and a Claude Design
prompt exist. The Detail Page shows the challenge type, plain-language
task description, in-scope and out-of-scope file/module sets (R8),
acceptance criteria, and a "new challenge" action to regenerate (R2).
It renders the **most-recent attempt as primary** and **prior attempts
inline (collapsible)** for self-review (R5 / FR-10). The page is the
host for the Debug Walkthrough UI (task 007) and the Completion Review
UI (task 008); the Page Spec resolves whether they sit inline or on a
sub-route.

## Acceptance Criteria
- [ ] Page Spec at `docs/design/challenge-detail-page.md` covering
      route
      `apps/web/app/repos/[owner]/[repo]/challenges/[challengeId]/page.tsx`,
      page purpose, data inputs (task-001 data-access layer), and
      data outputs (no writes from this page directly — submission
      goes through task 007's UI).
- [ ] The spec states the page renders, for the active challenge:
      type, plain-language task description, **in-scope file/module
      set** and **out-of-scope file/module set** (R8 / FR-3),
      acceptance criteria, and source references into the M6 project
      map.
- [ ] The spec specifies a **"new challenge" action** that triggers
      task 003's regeneration for the same type (R2 / FR-1).
- [ ] The spec specifies the **inline collapsible prior-attempts
      panel** (R5 / FR-10): most-recent attempt rendered as primary
      (free-text explanation + 0–100 score + weak-area breakdown
      summary), prior attempts listed below in a collapsed state with
      timestamps, expand/collapse client-side. Prior attempts are
      **not** on a separate page.
- [ ] The spec resolves whether the Debug Walkthrough UI (task 007)
      and the Completion Review UI (task 008) sit inline on this page
      or on a sub-route; the resolution is captured in this spec so
      task 007's and task 008's specs do not have to.
- [ ] Claude Design prompt under
      `docs/design/ui-prompts/challenge-detail-page.md` references the
      typed challenge shape (FR-3), the attempt + grading shape
      (FR-4, FR-5), and the latest-outcome / attempt-history accessor
      from the task-001 data-access layer so integration in task 009
      is unambiguous.
- [ ] **Claude Design (ADR 0007) is the UI generation tool — v0 is not
      used.**

## Technical Details

Per ADR 0007, every new page in M9 goes through the Claude Design
round-trip: Page Spec under `docs/design/` → prompt under
`docs/design/ui-prompts/` → Claude Design draft → integration notes
under `docs/design/ui-integration-notes/` at integration time
(task 009). v0 is **not** used in M9 (epic Architecture Decisions).

R5 — "most-recent attempt primary, prior attempts inline collapsible"
— is **specific to the Detail Page** and is the one bit that
distinguishes M9's Detail Page from a stock list-detail view. Make
this explicit in the prompt so the Claude Design output renders it
that way out of the box.

The "new challenge" action wires to task 003's regeneration path; the
spec only declares the action and the expected behavior (R2). The
integration in task 009 wires the action to the generation call.

Follow the page-spec / prompt conventions used by M4 / M5 / M6 / M8
(see `.claude/epics/archived/project-logic-mapper/107.md` and
`.claude/epics/archived/diff-review/115.md`). No code in this task.

## Dependencies
- ADR 0007 (Claude Design).
- PRD output shapes (FR-1, FR-3, FR-4, FR-5, FR-10).
- R2 (lazy regeneration), R5 (prior-attempts surfacing), R8 (in/out-of-scope source).
- Epic Technical Approach (Challenge Detail Page bullet).

## Effort Estimate
- Size: S
- Hours: 6

## Definition of Done
- [ ] Page Spec written and committed
- [ ] Claude Design prompt written and committed
- [ ] AI self-review complete
- [ ] No code is required for this task; `pnpm lint`, `pnpm typecheck`,
      `pnpm build`, `pnpm test` continue to pass with no API key set
- [ ] Spec reviewed
