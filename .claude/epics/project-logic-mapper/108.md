---
name: Integrate the Project Logic Mapper UI + Mermaid rendering
status: open
created: 2026-05-22T14:03:40Z
updated: 2026-05-22T14:52:00Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/108
depends_on: [105, 106, 107]
parallel: false
conflicts_with: []
---

# Task: Integrate the Project Logic Mapper UI + Mermaid rendering

## Description

Integrate the four Project Logic Mapper UI pieces into `apps/web`, wire them to
the LangGraph pipeline and the project-maps data-access layer, and render the
pipeline's Mermaid diagram source client-side.

## Acceptance Criteria
- [ ] Project Map page, Architecture Flow Viewer, File Map Explorer, and Debug
      Path UI integrated into `apps/web` and routed.
- [ ] Pages run the pipeline for a selected imported snapshot, persist the map
      via the data-access layer, and read it back.
- [ ] Mermaid diagram source from the pipeline renders client-side.
- [ ] File references shown in the UI pass the integrity check.
- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm test` pass with no API
      key set.

## Technical Details

Build from the task 007 page specs / Claude Design drafts. Add a client-side
Mermaid renderer. Routing additions to `apps/web` are additive; coordinate at
merge time with the parallel `diff-review` epic.

## Dependencies
- Task 005 (LangGraph pipeline).
- Task 006 (data-access layer).
- Task 007 (page specs + prompts).

## Effort Estimate
- Size: L
- Hours: 16

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
