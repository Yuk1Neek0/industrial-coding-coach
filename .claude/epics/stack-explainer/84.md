---
name: stack_explanations schema + migration
status: in-progress
created: 2026-05-22T00:20:11Z
updated: 2026-05-22T01:40:00Z
github: https://github.com/Yuk1Neek0/industrial-coding-coach/issues/84
depends_on: []
parallel: true
conflicts_with: []
---

# Task: stack_explanations schema + migration

## Description

Add a `stack_explanations` table to the existing SQLite database via Drizzle,
keyed by snapshot. The data-access layer builds on this; the detection and
explanation tasks are independent of it.

## Acceptance Criteria
- [ ] A `stack_explanations` table is added to `packages/db/src/schema.ts`,
      keyed by snapshot (`owner/repo` + ref), storing the structured explanation;
      list-valued fields as JSON columns.
- [ ] A Drizzle migration is generated and the snapshot/journal updated.
- [ ] No new database is created — the table joins the existing store.
- [ ] `pnpm typecheck` and the migration apply cleanly.

## Technical Details

`schema.ts` is shared with the parallel `recommendation-engine` epic — this epic
adds only the `stack_explanations` table; migration numbering is resolved at
merge time.

## Dependencies

None within the epic. Runs in parallel with tasks 2 and 5.

## Effort Estimate
- Size: M
- Hours: 5

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
