---
name: Portfolio Page — Page Spec + Claude Design prompt
status: open
created: 2026-05-27T13:32:28Z
updated: 2026-05-27T13:32:28Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Portfolio Page — Page Spec + Claude Design prompt

## Description

Author the Page Spec and Claude Design prompt for the single new
user-facing surface in M10 — the Portfolio Page at
`/portfolio/[owner]/[repo]`. Follows the Claude Design round-trip
(ADR 0007): Page Spec → prompt → Claude Design draft → integration
notes. v0 is not used.

The Page Spec must describe a single scrollable page composing
**all four artifacts in a fixed order** (per the M10 PRD), with anchor
nav, a "Regenerate memory" action, an "Export bundle" action (markdown
ZIP), an "Export PDF" action, and a stale-data banner (FR-11).

## Acceptance Criteria

- [ ] `docs/design/portfolio-page.page-spec.md` exists and covers:
      route + segment layout, data dependencies (the
      `learning_memories` row plus the M5/M6/M7/M8/M9 rows the
      composers read), section order (Architecture Explanation →
      Learning Memory Tree → Interview Q&A → Résumé Bullets → Debug
      Stories), per-section anchor link IDs, the three top-level
      actions (regenerate / export-markdown / export-PDF), the
      stale-data banner copy + trigger condition, and explicit empty /
      error / loading states.
- [ ] The Page Spec calls out the M7/M9-style "no API key required at
      view time" rule — the page renders cached memory; LLM calls only
      happen in Server Actions for regenerate.
- [ ] The Page Spec lists the *real* TypeScript shapes the page binds
      to (cross-reference task 001 for `InterviewQA`, `ResumeBullet`,
      etc.) so the Claude Design draft and the integration task can
      both lean on shipped types — guard against the M8 "spec ↔ shape
      drift" retro lesson.
- [ ] `docs/design/ui-prompts/portfolio-page.md` exists with a
      Claude Design prompt that references the Page Spec and produces
      a single page (no fragmenting into sub-routes per US-5).
- [ ] Integration-notes file is stubbed at
      `docs/design/ui-integration-notes/portfolio-page.md` with a
      "TODO — fill at task 9" placeholder so the round-trip lifecycle
      file exists from the start.
- [ ] Page Spec covers the "honest output" stance (FR-4) — weak-area
      items in the memory tree are surfaced, not hidden.

## Technical Details

- Mirror the shape of the M9 page specs
  (`docs/design/challenge-list-page.md` and friends): include the
  data-loading paragraph, the per-section paragraph, the actions list,
  and the empty/loading/error matrix.
- The Page Spec is the single source of truth for the page's API
  surface — the integration in task 9 must match it.
- No code in this task — Page Spec + prompt + stub only.

## Dependencies

None in-epic. References shipped data shapes from M5/M6/M7/M8/M9 and
will reference task 001's exported types as those land.

## Effort Estimate

- Size: S
- Hours: 4

## Definition of Done

- [ ] Page Spec written
- [ ] Claude Design prompt written
- [ ] Integration-notes stub created
- [ ] Spec reviewed
