// Drizzle schema for the Golden Path Catalog — local SQLite (ADR 0006).
//
// The `golden_paths` table is defined in task #30 (Golden Path schema +
// migration). This file is intentionally present so `drizzle.config.ts` and
// `client.ts` resolve before the schema lands.
export {}
