# Backstage software template — adapted as an in-repo fixture for the M14 importer.
# Source: backstage/software-templates (official), structure-faithful (trimmed).
# https://github.com/backstage/software-templates/tree/main/scaffolder-templates/springboot-grpc-template
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: springboot-grpc-template
  title: Spring Boot gRPC Service
  description: Create a Spring Boot gRPC microservice with Gradle and a CI workflow.
  tags:
    - recommended
    - java
    - spring-boot
    - grpc
  annotations:
    backstage.io/source-location: url:https://github.com/backstage/software-templates/tree/main/scaffolder-templates/springboot-grpc-template/
spec:
  owner: backend@backstage.io
  type: service
  parameters:
    - title: Provide component information
      required:
        - component_id
        - owner
      properties:
        component_id:
          title: Name
          type: string
          description: Unique name of the component
        owner:
          title: Owner
          type: string
          description: Owner of the component
    - title: Choose a location
      required:
        - repoUrl
      properties:
        repoUrl:
          title: Repository Location
          type: string
  steps:
    - id: fetch-base
      name: Fetch Base
      action: fetch:template
      input:
        url: ./skeleton
    - id: publish
      name: Publish
      action: publish:github
      input:
        repoUrl: "{{ parameters.repoUrl }}"
    - id: register
      name: Register
      action: catalog:register
      input:
        catalogInfoPath: /catalog-info.yaml
