# Progress checkpoint — 2026-06-02

Session-end snapshot. **State of the world:** **M12 (CCPM Integration) is
complete and merged to `main`**; **M13 (LLM Observability) is planned, synced to
GitHub, and has a ready execution worktree — Wave 1 has not started.**

## Repo state on `origin`

| | |
|---|---|
| `main` | `9d763a6` — clean, up to date |
| `epic/llm-observability` | base for M13 task PRs (worktree below); no task work yet |
| Worktree | `../epic-llm-observability` on `epic/llm-observability`, clean, planning files present |
| Open PRs | 0 |

Remote branches: `origin/main`, `origin/epic/llm-observability` only.

## M12 — CCPM Integration ✅ COMPLETE

Epic [#196](https://github.com/Yuk1Neek0/industrial-coding-coach/issues/196)
(CLOSED) · PRD `.claude/prds/ccpm-integration.md` · Epic archived to
`.claude/epics/archived/ccpm-integration/` · Status doc
`docs/milestones/M12-ccpm-integration.md`.

Turns a repo's CCPM artifacts into a local **delivery traceability map**
(PRD → Epic → Task → Issue → PR) + deterministic teaching, with graceful
degradation for non-CCPM repos. Local-first, read-only. New page
`/delivery/[owner]/[repo]`; new `packages/db/src/ccpm/` module; migration `0010`.

| Issue | Task | PR |
|---|---|---|
| #198 | Generalized CCPM parser | #207 |
| #199 | Snapshot coverage for `.claude/**` | #208 |
| #197 | `ccpm_issue_links` schema + migration 0010 | #209 |
| #200 | Traceability graph + detection + degradation | #210 |
| #201 | Import-time issue/PR linking | #211 |
| #202 | Deterministic teaching layer | #212 |
| #203 | Data-access `getDeliveryMap` | #213 |
| #204 | Page Spec + Claude Design prompt | #214 |
| #205 | Integrate `/delivery/[owner]/[repo]` | #215 |

Epic merged via **PR #216**; archived via **PR #217**. All task PRs CI-green.

## M13 — LLM Observability 🟡 PLANNED + SYNCED (Wave 1 not started)

Epic [#218](https://github.com/Yuk1Neek0/industrial-coding-coach/issues/218)
(OPEN) · PRD `.claude/prds/llm-observability.md` · Epic
`.claude/epics/llm-observability/epic.md` · Planning landed via **PR #228**.

A local-first, **Langfuse-shaped, zero-Langfuse-dependency** observability layer:
**Part A** instruments the coach's own bounded SDK calls (M7/M9/M10) with traces
+ evals (eval **reuses the existing integrity checks** — no new LLM call);
**Part B** detects + teaches the imported repo's observability story. One page
`/observability/[owner]/[repo]`. New module `packages/db/src/observability/`.

**Key architecture (for resuming):**
- Instrument at the `@workspace/ai` `LlmClient.complete()` seam via a recording
  decorator `createObservedLlmClient(...)` — the client already returns token
  `usage`; multi-turn tool-use calls map to Langfuse trace→observation→score.
  M7/M9/M10 change one line (client construction) + one `recordEval` after their
  integrity check. **All writes best-effort / non-blocking** (zero behavior change).
- Part B is a pure analyzer over `repo_files`, derived-on-read (no persistence),
  mirroring the M12 graph/teaching pattern.

| Issue | Task | deps | Wave |
|---|---|---|---|
| #219 | Observability schema + migration `0011` | — | 1 |
| #220 | Per-model cost estimator | — | 1 |
| #221 | Repo observability analyzer (Part B detect) | — | 1 |
| #222 | Recording decorator + recordEval | 219,220 | 2 |
| #223 | Part-B teaching | 221 | 2 |
| #224 | Instrument M7/M9/M10 bounded calls | 222 | 3 |
| #225 | Data-access `getObservability` | 219,221,223 | 3 |
| #226 | Page Spec + Claude Design prompt | 225 | 3 |
| #227 | Integrate `/observability/[owner]/[repo]` | 225,226 | 3 |

## What's next (resume here)

1. **Start M13 Wave 1** — `start working on issue 219` (schema + migration,
   foundational) or `221` (pure Part-B analyzer). Wave 1 = #219 / #220 / #221,
   no deps, conflict-free.
2. **Execution cadence (as used through M12):** for each task — in the worktree
   `../epic-llm-observability`, branch `task/<n>-<slug>` off `epic/llm-observability`
   → implement → `pnpm --filter <pkg> typecheck/test/lint` (+ `build` for web) →
   commit `Issue #<n>: …` → task PR into `epic/llm-observability` → CI → merge →
   close issue + bump epic progress → stop for review.
3. After all 9 tasks: epic PR `epic/llm-observability` → `main`, then archive +
   write `docs/milestones/M13-llm-observability.md` + close epic #218.

## Milestone map

M0–M12 complete (see `docs/milestones/README.md`). **M13 in flight.** Remaining
planned milestones: M14 (Backstage / Golden Path source import), M15 (Team /
Classroom mode) — per `updated_ai_native_milestone_plan.md`.
