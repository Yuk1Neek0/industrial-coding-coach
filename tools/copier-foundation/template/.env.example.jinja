# Environment variable template for {{ project_name }} — committed.
# Copy to `.env` (git-ignored) and fill in real values locally.
# Never commit real secrets. Keys here must stay as empty placeholders.

# Add variables below as features introduce them, e.g.:
# DATABASE_URL=
# API_KEY=
# NEXT_PUBLIC_APP_NAME={{ project_name }}
